services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "127.0.0.1:5433:5432"
    environment:
      POSTGRES_USER: simplo
      POSTGRES_PASSWORD: simplo
      POSTGRES_DB: simplo_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
